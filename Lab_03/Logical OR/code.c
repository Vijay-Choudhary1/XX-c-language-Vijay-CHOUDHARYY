#include<stdio.h>
int main () {
    int a = 0;
    int b = 20;
    printf("%d\n", a> 15 || b> 18);
    printf("%d", a < 2 || b> 18);
    return 0;
}

// if any conditions is true then print should be 1, if non of the conditions are true print should be 0 
    // 1- true
    // 0 - false
